@model Challange

<h2>Join @Model.Title</h2>
<hr />
<h4>@($"{Model.Start:MM/dd/yyy}") - @($"{Model.End:MM/dd/yyy}")</h4>
<form asp-action="Join" asp-route-id="@Model.Id">
    <partial name="_JoinForm" model="new Participant()" />
</form>